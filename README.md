# Relevant Priors Endpoint

POST /predict endpoint for the New Lantern Residency challenge.

## Local development
```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
pytest -x
```

## Status
Built in 7 chunks. Currently at: Chunk 1 — scaffold + normalizer + features.
